import StudiesPage from "@/app/components/StudiesPage";

export const metadata = { title: "Public Studies — Samply" };

export default function StudiesIndexPage() {
  return <StudiesPage page={1} />;
}
