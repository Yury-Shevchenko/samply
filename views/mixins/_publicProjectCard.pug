mixin publicProjectCard(project = {})
  a(href=`/studies/${project.slug}`)
    .project
      .project__hero
        h2.project__title #{project.name}
      .project__details
        if(project.description.split(' ').length > 30)
          p(style="font-size:1.5rem")= `${project.description.split(' ').slice(0, 30).join(' ')}...`
        else 
          p(style="font-size:1.5rem")= project.description
      .project__bottom
        p(style="font-size:1rem; font-style: italic;")= project.author_name
        p(style="font-size:1rem")= project.author_institute

      //- if(user && user.level < 10)
      //-   if(user.participant_projects && user.participant_projects.map(e => e._id.toString()).includes(project._id.toString()))
      //-     a(href=`/testing/${project._id}`)
      //-       .button(style="background-color:#ed1a1a; width: fit-content; color:white;") #{layout.unsubscribe} 
      //-   else 
      //-     a(href=`/testing/${project._id}`)
      //-       .button(style="width: fit-content;") #{layout.subscribe_to} 
      
    
    
