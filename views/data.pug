extends layout

include mixins/_userCard

block content
  .inner

    .userTable
      h2 #{l.title}

      if(project && user.project && user.project.name)

        .card
          .users
            table.table
              thead
                td №
                td #{l.table_samplyId}
                td #{l.table_date}
                td #{l.table_history}
                td #{l.table_new_notification}
                if user.level > 10
                  td <i class="fas fa-ban"></i>
              
              if(project.mobileUsers)  
                each person, i in project.mobileUsers
                  +userCard(person, i)

      else
        p !{l.message_create_project}
